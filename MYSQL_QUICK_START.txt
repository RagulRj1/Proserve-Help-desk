================================================================================
  🗄️ MySQL Migration - Quick Start Guide
================================================================================

You have MySQL 8.0 installed! Let's migrate from SQLite to MySQL.

================================================================================
  ⚡ AUTOMATIC MIGRATION (Recommended - 2 Minutes)
================================================================================

Run these 3 commands:

1. STOP SERVERS:
   .\STOP_SERVERS.bat

2. RUN MYSQL SETUP:
   .\SETUP_MYSQL.bat
   
   When prompted:
   - Enter MySQL root password
   - Press Enter to continue

3. START SERVERS:
   .\START_BOTH_SERVERS.bat

DONE! Your data is now in MySQL! ✅

================================================================================
  📋 WHAT THE SETUP DOES
================================================================================

1. ✅ Installs Python MySQL driver (pymysql)
2. ✅ Creates MySQL database: helpdesk_db
3. ✅ Creates MySQL user: helpdesk_user
4. ✅ Creates all tables (users, tickets)
5. ✅ Migrates your 5 users from SQLite
6. ✅ Migrates your 2 tickets from SQLite
7. ✅ Updates configuration to use MySQL
8. ✅ Backs up old SQLite config

================================================================================
  🔑 DEFAULT CREDENTIALS
================================================================================

MySQL Database Credentials:
Host:     localhost
Port:     3306
Database: helpdesk_db
User:     helpdesk_user
Password: HelpDesk@2024

Application Login (changeable):
Username: admin
Password: admin123

================================================================================
  ✅ AFTER SETUP
================================================================================

Check Backend Terminal:
Should show: [Database] Using MySQL/PostgreSQL: localhost:3306/helpdesk_db

Login to Application:
1. Open http://localhost:3000
2. Login: admin / admin123
3. Check User Management → Should see 5 users ✅
4. Check All Tickets → Should see 2 tickets ✅

================================================================================
  🔍 VERIFY DATA IN MYSQL
================================================================================

Option 1: Using MySQL Command Line
----------------------------------
mysql -u helpdesk_user -p
# Password: HelpDesk@2024

USE helpdesk_db;
SELECT * FROM users;
SELECT * FROM tickets;

Option 2: Using MySQL Workbench
--------------------------------
1. Open MySQL Workbench
2. Connect to: helpdesk_user@localhost
3. Password: HelpDesk@2024
4. Browse helpdesk_db → Tables

================================================================================
  🐛 TROUBLESHOOTING
================================================================================

Problem: "Access denied"
Fix: Check MySQL password. Default is: HelpDesk@2024

Problem: "Can't connect to MySQL server"
Fix: Start MySQL service
     net start MySQL80

Problem: "Unknown database"
Fix: Run this in MySQL:
     CREATE DATABASE helpdesk_db;

Problem: Backend still shows "Using SQLite"
Fix: Check backend/.env file
     Should have: DATABASE_URL=mysql+pymysql://...

Problem: "No module named pymysql"
Fix: cd backend
     pip install pymysql cryptography

================================================================================
  📁 FILES CREATED
================================================================================

✅ backend/setup_mysql.sql       → Creates database and user
✅ backend/.env.mysql            → MySQL configuration template
✅ backend/migrate_to_mysql.py   → Data migration script
✅ SETUP_MYSQL.bat               → Automated setup script
✅ MYSQL_MIGRATION_GUIDE.md      → Full documentation
✅ backend/.env.sqlite.backup    → Backup of old config (after setup)

================================================================================
  🔄 ROLLBACK TO SQLITE (If Needed)
================================================================================

If you want to go back to SQLite:

1. Stop servers:
   .\STOP_SERVERS.bat

2. Restore SQLite config:
   cd backend
   copy .env.sqlite.backup .env

3. Start servers:
   cd ..
   .\START_BOTH_SERVERS.bat

Your SQLite database (helpdesk.db) is still there!

================================================================================
  🎯 BENEFITS OF MySQL
================================================================================

✅ Better performance with multiple users
✅ Handles thousands of tickets easily
✅ Production-ready and scalable
✅ Better concurrent access
✅ Enterprise backup solutions
✅ Network access capabilities

================================================================================
  💡 SECURITY TIPS
================================================================================

Change Default Password:
-----------------------
ALTER USER 'helpdesk_user'@'localhost' IDENTIFIED BY 'YourNewPassword';

Then update backend/.env:
DATABASE_URL=mysql+pymysql://helpdesk_user:YourNewPassword@localhost:3306/helpdesk_db

Regular Backups:
----------------
mysqldump -u helpdesk_user -p helpdesk_db > backup.sql

Restore from Backup:
--------------------
mysql -u helpdesk_user -p helpdesk_db < backup.sql

================================================================================
  📊 YOUR CURRENT DATA
================================================================================

From SQLite (will be migrated):
• 5 Users (admin, manager, technician, user, +1)
• 2 Tickets (in_progress status)

After Migration:
All data preserved in MySQL ✅

================================================================================
  🚀 READY TO MIGRATE?
================================================================================

Run these 3 commands:

.\STOP_SERVERS.bat
.\SETUP_MYSQL.bat         ← Enter MySQL root password when asked
.\START_BOTH_SERVERS.bat

That's it! Your Help Desk is now on MySQL! 🎉

For detailed guide: See MYSQL_MIGRATION_GUIDE.md

================================================================================
